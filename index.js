const audio = document.getElementById('myAudio');

// Adicionar um evento de clique ao elemento desejado (por exemplo, um botão ou a própria tela)
document.addEventListener('click', () => {
    // Reproduzir a música
    audio.play();
});